// Tooltip Module
window.NFTApp.registerModule("tooltipHandler", {
  // Set up the tooltip handler
  setup: () => {
    console.log("Setting up tooltip handler module")
    // The actual functionality is now in tooltip-handler-consolidated.js
    // This module is just a placeholder for the module system
  },
})
